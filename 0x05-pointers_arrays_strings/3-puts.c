#include "main.h"
/**
* _puts - Prints a string
* @str: string
*
* Description: Prints a string followed by a newline to stdout
*
* Return: nothing
**/
void _puts(char *str)
{
	while
	(*str != '\0');
	_putchar(*str);
	str++;
	_putchar('\n');
}
